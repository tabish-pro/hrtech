# Copy this file to `.env` and fill in the real values. Do NOT commit your real `.env`.

# Required - OpenRouter API key (must start with sk-or-)
OPENROUTER_API_KEY=sk-or-your-openrouter-key-here

# Optional - SendGrid API key (if you use email features)
SENDGRID_API_KEY=SG-your-sendgrid-key-here

# SendGrid sender email address (must be verified in SendGrid)
SENDGRID_FROM_EMAIL=your_verified_sender@domain.com

# Default admin password for initial setup (change this to a secure password!)
DEFAULT_ADMIN_PASSWORD=your_secure_admin_password

# PostgreSQL database settings
POSTGRES_DB=resume_analyzer_db
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_secure_db_password

# Database connection string used by the application
# NOTE: When using `docker compose` the database host is `db` (as in the compose file).
# If you run `node server.js` locally without Docker, change the host to `localhost` or
# point to a running Postgres instance on your machine, e.g.:
# DATABASE_URL=postgresql://user:password@localhost:5432/resume_analyzer_db
DATABASE_URL=postgresql://your_db_user:your_secure_db_password@db:5432/resume_analyzer_db?sslmode=disable

# Other optional runtime settings
NODE_ENV=development
PORT=3000

# SSL/HTTPS Configuration (required for production)
DOMAIN_NAME=hrtech.yourdomain.com
SSL_EMAIL=admin@yourdomain.com
STAGING=1  # Use 1 for testing, 0 for production certificates

# Security note: keep `.env` outside of source control and never paste secrets into public discussions.
